# FiscAudit AI - Python Requirements
#
# Only the packages the code actually imports. Everything else in the previous
# version (pandas, numpy, pymupdf, PyPDF2, pdf2image, sqlalchemy, cryptography,
# python-docx, httpx, aiohttp, requests, tenacity, ...) was unused and was the
# main source of install failures: pymupdf and cryptography compile from source,
# and pdf2image needs the poppler system binary that is not present on
# Streamlit Cloud.
#
# PDF bytes are sent straight to Gemini as base64, so no local PDF library is
# needed. pandas arrives transitively via streamlit.
#
# IMPORTANT: set the Python version to 3.11 or 3.12 in Streamlit Cloud
# (Advanced settings when deploying). The build log showed Python 3.14, for
# which several wheels do not yet exist.

streamlit>=1.32,<2
google-generativeai>=0.3
anthropic>=0.34
supabase>=2.4
pydantic>=2.5,<3
